<%= form_for(@node_feed) do |f| %>
  <% if @node_feed.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@node_feed.errors.count, "error") %> prohibited this node_feed from being saved:</h2>

      <ul>
      <% @node_feed.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
